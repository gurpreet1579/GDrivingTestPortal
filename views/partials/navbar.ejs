<nav class="navbar navbar-expand-lg  nav-bg">
    <div class="container-fluid">
        <a class="navbar-brand txt-color" href="/home">D-License</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
        <div class="collapse navbar-collapse justify-end" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link  txt-color" aria-current="page" href="/home">Home</a>
                </li>
                <% if (session.userId && session.userType === 'Admin') { %>
                    <li class="nav-item">
                        <a class="nav-link  txt-color" aria-current="page" href="/admin/appointment">Appointment</a>
                    </li>
                <% } %>
                <% if (session.userId && session.userType === 'Driver') { %>
                    <li class="nav-item">
                        <a class="nav-link  txt-color" aria-current="page" href="/g2">G2_Test</a>
                    </li>
                <% } %>
                <% if (session.userId && session.userType === 'Driver' ) { %>
                    <li class="nav-item">
                        <a class="nav-link txt-color" href="/g">G_Test</a>
                    </li>
                <% } %>
                <% if (session.userId && session.userType === 'Examiner' ) { %>
                    <li class="nav-item">
                        <a class="nav-link txt-color" href="/examiner/schedule">Schedule</a>
                    </li>
                <% } %>
                <% if (session.userId && session.userType === 'Admin' ) { %>
                    <li class="nav-item">
                        <a class="nav-link txt-color" href="/admin/result">Result</a>
                    </li>
                <% } %>
                <% if (!session.userId ) { %>
                    <li class="nav-item">
                        <a class="nav-link txt-color" href="/login">Log In</a>
                    </li>
                <% } %>
                <% if (!session.userId) { %>
                    <li class="nav-item">
                        <a class="nav-link txt-color" href="/user/sign-up">Sign Up</a>
                    </li>
                <% } %>
                <% if (session.userId) { %>
                    <li class="nav-item">
                        <a class="nav-link txt-color" href="/user/destroy-session">Log Out</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>